{
  "name": "@prompt-gloss/server",
  "version": "0.1.0",
  "description": "Fastify server for Gloss: Agent SDK session, REST + SSE API, injection pipeline.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "gloss-server": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "node --watch --experimental-strip-types src/main.ts",
    "start": "node dist/main.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.207",
    "@prompt-gloss/core": "workspace:*",
    "fastify": "^5.2.1"
  }
}
