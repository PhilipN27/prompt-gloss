{
  "name": "@prompt-gloss/hook",
  "version": "0.1.0",
  "description": "Claude Code UserPromptSubmit/SessionStart hook for Gloss. Ships as a single esbuild CJS bundle.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run --project hook"
  },
  "dependencies": {
    "@prompt-gloss/core": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
